<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.hospital.model.mapper.UserAccountMapper">

    <select id="findByUsername">
        select user_no,
               username,
               password,
               role,
               status,
               pw_changed_at,
               last_access_at,
               created_at,
               updated_at
        from user_account
        where username = #{username}

    </select>

    <insert id="existsByUsername">
        select exists (select 1
                       from user_account
                       where username = #{username})
    </insert>

    <update id="rotateRefreshToken">
        update user_session
        set jti        = #{newJti},
            issued_at  = #{issuedAt},
            expires_at = #{expiresAt},
            ip         = #{ip},
            user_agent = #{userAgent}
        where jti = #{oldJti}
          and device_id = #{deviceId}
          and revoked = 0
          and expires_at > now(3)
    </update>

    <update id="revokeByJti">
        update user_session
        set revoked = 1
        where jti = #{jti}
          and revoked = 0 -- 이미 취소된 건 건드리지 않음
    </update>

</mapper>